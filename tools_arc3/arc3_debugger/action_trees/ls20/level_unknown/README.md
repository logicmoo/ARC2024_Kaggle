# `ls20` — Level `?`

- [Initial state](image_9c879087889c0ade/README.md)

This directory is a deterministic action tree. Each state directory
contains `image.png`, `state.json`, `README.md`, and cached Prolog
analysis. Each `actions/<action>/` directory leads to the resulting
state node.
